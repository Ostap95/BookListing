<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_view_item_weight"
    android:paddingTop="@dimen/list_view_padding_top"
    android:paddingBottom="@dimen/list_view_padding_bottom"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginLeft="@dimen/book_item_text_left_margin"
        android:layout_marginRight="@dimen/book_item_text_right_margin"
        >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/book_title"
            android:maxLines="1"
            android:fontFamily="sans-serif"
            android:textSize="@dimen/list_item_title_text_size"
            android:textStyle="bold"
            tool:text="Title"/>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/book_author"
            android:fontFamily="sans-serif"
            android:textSize="@dimen/list_item_author_text_size"
            tool:text="Author"/>

    </LinearLayout>



</RelativeLayout>